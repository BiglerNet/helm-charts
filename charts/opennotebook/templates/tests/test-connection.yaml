apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opennotebook.fullname" . }}-test-connection"
  labels:
    {{- include "opennotebook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opennotebook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
